const path = require('path');

module.exports = {
    /**
    * @const pathTemp - дефолтная директива куда создаются генерируемые файлы папки и тд
    */
    pathTemp: path.resolve(__dirname, "../../temp")
}